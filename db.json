{ "auth": {
        "identity": {

          # Include both password and totp methods
            "methods": [
                "password",
                "totp"
            ],
            
            # TOTP (New method)
            "totp": {
                "user": {
                    "name": "firstname.surname@email.com",
                    "domain:" { "id": "default" },
                    "passcode": "012345"
                }
            },

            # Password (Already exists)
            "password": {
                "user": {
                    "name": "firstname.surname@email.com",
                    "domain:" { "id": "default" },
                    "password": "UserDefinedPassword"
                }
            }
        }
    }
}
